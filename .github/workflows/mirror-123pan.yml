name: Mirror to 123pan (离线下载)

on:
  workflow_dispatch:
    inputs:
      client_id:
        description: "123云盘 OpenAPI Client ID"
        required: true
      client_secret:
        description: "123云盘 OpenAPI Client Secret"
        required: true

permissions:
  contents: write

jobs:
  mirror:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Mirror via offline download
        run: node scripts/mirror-to-123pan.js
        env:
          PAN123_CLIENT_ID: ${{ github.event.inputs.client_id }}
          PAN123_CLIENT_SECRET: ${{ github.event.inputs.client_secret }}

      - name: Commit updated index
        if: always()
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update 123pan share links"
          file_pattern: |
            public/index.json
            public/.shares.json